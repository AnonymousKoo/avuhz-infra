{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:avuhz:schema:contracts:identity:capability:v1",
  "title": "Avuhz Slice 1 Capability v1",
  "description": "Closed vocabulary for a bounded action claim. A capability does not establish tenant membership or human authority; caller type and capability remain separate concepts. scope:approve does not create a HumanApproval record, and event:publish_internal is reserved for a trusted outbox publisher.",
  "type": "string",
  "minLength": 10,
  "maxLength": 25,
  "enum": [
    "engagement:read",
    "engagement:accept_handoff",
    "engagement:open",
    "scope:read",
    "scope:submit",
    "scope:approve",
    "approval:create",
    "inbound_event:record",
    "event:publish_internal"
  ]
}
